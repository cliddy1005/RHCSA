# Copy to config/secrets.yml and edit locally.
ssh:
  username: student
  private_key: ~/.ssh/id_rsa
rhel:
  # Optional credentials for subscription or local mirrors if your environment requires it.
  username: "YOUR_USERNAME"
  password: "YOUR_PASSWORD"
